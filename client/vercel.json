{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "https://mwanzo-tunes-server.onrender.com/api/$1" },
    { "src": "/(.*)", "dest": "/dist/index.html" }
  ]
}